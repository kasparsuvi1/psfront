import { StateTranslaterPipe } from './state-translater.pipe';

describe('StateTranslaterPipe', () => {
  it('create an instance', () => {
    const pipe = new StateTranslaterPipe();
    expect(pipe).toBeTruthy();
  });
});
